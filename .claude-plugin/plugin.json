{
  "name": "ytmp4-ai-digest",
  "description": "Browse subscribed YouTube channels, fetch transcripts, and generate Markdown summary digests. Supports multi-video comparison with interactive dashboard, frame screenshots, and chat.",
  "version": "2.0.0",
  "author": {
    "name": "TheDigitalGriot"
  },
  "keywords": ["youtube", "video", "digest", "transcript", "summary", "comparison", "analysis"],
  "userConfig": {
    "anthropic_api_key": {
      "type": "string",
      "title": "Anthropic API Key",
      "description": "Anthropic API key — fallback for the in-viewer chat when the Claude CLI isn't available (e.g. on Cowork). Leave blank to use your Claude subscription via the CLI.",
      "sensitive": true
    },
    "local_base_url": {
      "type": "string",
      "title": "Local Model Base URL",
      "description": "OpenAI-compatible base URL for local/custom chat models (e.g. http://localhost:11434/v1 for Ollama). Optional.",
      "sensitive": false
    },
    "local_model": {
      "type": "string",
      "title": "Local Model Name",
      "description": "Model name for the local/custom OpenAI-compatible endpoint (e.g. your fine-tune, gemma3, kimi-k2). Optional.",
      "sensitive": false
    }
  }
}

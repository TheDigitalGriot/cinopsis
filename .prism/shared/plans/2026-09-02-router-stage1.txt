Read the file .prism/shared/plans/2026-09-02-cinopsis-cookie-and-vault-CONTEXT.md in full, then execute STAGE I (implement) followed by STAGE V (validate) exactly as that contract specifies.
Use the prism code-intel agents graph-navigator and codebase-analyzer to locate call sites and data flow; do not photocopy whole files.
Follow prism:cl-plugin-structure: scripts/ placement, NO hardcoded paths (use ${CLAUDE_PLUGIN_DATA} via _utils), and `claude plugin validate .` must pass.
Do NOT run any youtube, yt-dlp, or network calls anywhere — the residential IP is flagged; all backfill assembles from cached data only, and validation is offline.
Make all edits directly in the repo. Print each heartbeat token on its own line as you finish that step: HB:I1-exporter, HB:I2-reindex, HB:I3-backfill, HB:I4-vault, HB:V-validate, then HB:DONE.
When finished, print a concise summary: every file created or changed, and the validation results.
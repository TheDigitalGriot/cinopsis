You are running headless with the working directory C:\Users\digit\GriotApps\Cinopsis.

Read the stage contract at .\CONTEXT-cinopsis.md and execute it exactly.

Cinopsis is a shipped, working product — work ADDITIVELY and do not break the MCP server, the viewer, or
the launcher anti-orphan hygiene. Do an ICM pass (CLAUDE.md routing entry + .prism stage-CONTEXT template +
code-intel stub) and add a passive file-bus channel surface (scripts/channel_bus.py) wiring the Cinopsis
verbs onto the shared Griot MCP bus pattern additively, keeping the existing stdio MCP tools intact. Verify
syntax and that existing tools are still registered. Commit once with a plain conventional-commit message and
no AI attribution. Proceed autonomously; do not ask questions.

Append a timestamped heartbeat line to .\.cinopsis-icm-progress.txt after each numbered step using the tokens
in the contract. Append a final DONE line with the commit sha.
